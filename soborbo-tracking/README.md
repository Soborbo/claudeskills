# soborbo-tracking — moved

This package was consolidated into the engine repo on **2026-07-21**.

➡️ **Canonical home:** `github.com/Soborbo/Serverside` → `soborbo-tracking/`

There is no longer a vendored `events.json` copy here — the gateway worker and the
client lib share the single canonical `Serverside/src/events.json`. See
[`SKILL.md`](./SKILL.md) for details.
