/**
 * Stub for `cloudflare:workers`. The real module is only available inside
 * a Workers/Pages runtime; in tests we expose an `env` placeholder that
 * tests can override with `vi.mock('cloudflare:workers', ...)`.
 */
export const env: Record<string, string | undefined> = {};
